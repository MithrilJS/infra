on:
  schedule:
    - cron: '0 0 * * *'

permissions:
  issues: write

jobs:
  remind:
    runs-on:
    - ubuntu-latest
    - windows-latest
    - macos-latest
    steps:
    - uses: MithrilJS/infra/internal/remind@main
